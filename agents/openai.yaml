interface:
  display_name: "UIQA"
  short_description: "审查 UI 交付质量、响应式与业务体验"
  default_prompt: "Use $uiqa to review this UI delivery for responsive, component, visual, and business UX issues."
policy:
  allow_implicit_invocation: true
